diff --git a/finetune_multigpu.py b/finetune_multigpu.py
index b3d514a..54cc0fb 100644
--- a/finetune_multigpu.py
+++ b/finetune_multigpu.py
@@ -299,6 +299,7 @@ def finetune(model_path:str, model_size: str, num_fingerprints: int, max_key_len
         ddp_find_unused_parameters=False,
         gradient_accumulation_steps=gradient_accumulation_steps,  # Increase gradient accumulation steps
         bf16=True,
+        optim="adamw_bnb_8bit",  # 8-bit Adam states drastically cut the DeepSpeed CPU-offloaded optimizer footprint
         dataloader_pin_memory=True,
         dataloader_num_workers=2,
         save_strategy="no",
